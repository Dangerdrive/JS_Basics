
//javascript converts 'loose' equality
console.log(1 == '1')
//so the example above turns to 'truth'

//to make it not ambiguos(strict equality) we can use ===
console.log(1 === '1')
//prints 'false'
